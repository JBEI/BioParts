package org.abf.bps.lib.common;

public enum ConfigProperty {

    // location of data directory
    DATA_DIR,

    // blast installation location (must point to the "bin" folder)
    BLAST_INSTALL,
    REGISTRY_TOKEN,
    REGISTRY_TOKEN_CLIENT,
    REGISTRY_TOKEN_OWNER,
    ADDGENE_URL_PREFIX,
    // url prefix for retrieving the part information from IGEM in xml format
    IGEM_XML_PART_URL_PREFIX,
    IGEM_PART_URL_PREFIX,
    NCBI_SEARCH_URL,
    MASTER_REGISTRY_URL,
    SYNBIO_HUB_URL
}
